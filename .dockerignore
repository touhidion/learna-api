.git
.gitignore
# Never let a real secret into an image layer. The example is excluded too:
# nothing in the container reads it.
.env
.env.*
bin/
tmp/
docs/
*.md
Dockerfile
docker-compose.yml
.idea/
.vscode/

# Local noise that must not invalidate the build cache.
*.log
.gitattributes
coverage.*
